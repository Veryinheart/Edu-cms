export const API_URL = 'http://cms.chtoma.com/api';

export enum QueryPath {
  login = 'login',
  logout = 'logout',
  students = 'students',
}

export enum ActionPath {
  get = 'get',
  post = 'post',
}
